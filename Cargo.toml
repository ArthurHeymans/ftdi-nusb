[package]
name = "ftdi-nusb"
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Pure Rust library for communicating with FTDI USB devices, backed by nusb"
repository = "https://github.com/ArthurHeymans/ftdi-nusb"
keywords = ["ftdi", "usb", "serial", "mpsse", "bitbang"]
categories = ["hardware-support", "embedded"]

[features]
default = ["std"]
std = ["is_sync", "nusb", "thiserror"]
is_sync = ["maybe-async/is_sync"]
wasm = [
  "nusb",
  "thiserror",
  "web-sys",
  "wasm-bindgen",
  "wasm-bindgen-futures",
  "js-sys",
]
embedded-hal = ["dep:embedded-hal", "dep:embedded-io"]

[package.metadata.docs.rs]
# WebUSB bindings require the unstable web_sys APIs cfg.
all-features = true
rustc-args = ["--cfg=web_sys_unstable_apis"]

[dependencies]
nusb = { version = "0.2.6", optional = true }
thiserror = { version = "2", optional = true }
log = "0.4"
maybe-async = "0.2"

# WASM / WebUSB dependencies. WebUSB types require building with
# RUSTFLAGS='--cfg=web_sys_unstable_apis'.
web-sys = { version = "0.3", optional = true, features = [
  "Navigator",
  "Window",
  "WorkerGlobalScope",
  "console",
  "Usb",
  "UsbDevice",
  "UsbDeviceFilter",
  "UsbDeviceRequestOptions",
] }
wasm-bindgen = { version = "0.2", optional = true }
wasm-bindgen-futures = { version = "0.4", optional = true }
js-sys = { version = "0.3", optional = true }

embedded-hal = { version = "1.0.0", optional = true }
embedded-io = { version = "0.7.1", optional = true }

[dev-dependencies]
env_logger = "0.11"
proptest = "1"
